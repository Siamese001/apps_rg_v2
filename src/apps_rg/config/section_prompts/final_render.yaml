# Final render section prompt profile ΓÇö W3
# Plan: apps-rg-zip-based-full-spine-runtime-restoration-a3f7e2
# GOVERNANCE: Declarative data only. No prompt compiler logic here.
prompt_id: "sp::apps_rg::final_render::v1"
node_id: final_render
app_id: apps_rg
task_class: resume_generation
version: "1.0.0"
status: active

slot_requirements:
  required: [S0, C0, D0, R0]
  optional: []

template_ref: "apps_rg/prompt_assembly/templates/docx_manifest_v1.yaml"
template_inline_note: >
  Final render assembles all section outputs into the sealed resume artifact.
  Format rendering only ΓÇö no new content generation.
  Slot C0 carries the assembled section artifacts from upstream nodes.

input_refs:
  - "header_block_output (from header_block node)"
  - "professional_summary_output (from professional_summary node)"
  - "skills_block_output (from skills_block node)"
  - "experience_block_output (from experience_block node)"
  - "education_block_output (from education_block node)"
  - "certifications_block_output (from certifications_block node, if present)"
  - "selected_projects_block_output (from selected_projects_block node, if present)"

output_schema_ref: "aos::apps_rg::resume_generation::v1"

required_evidence_refs:
  - "sealed_section_artifact_refs from all upstream generation nodes"
  - "evidence_digest from C0 receipt"

prompt_boundary_rules:
  - "final_render MUST NOT generate new content ΓÇö assembly and format only"
  - "no additional sections not produced by upstream nodes"
  - "section ordering MUST follow output_schema_ref required_sections order"
  - "output_artifact_digest MUST be computed and sealed before X3 Exit"
  - "all sealed_section_artifact_refs MUST be recorded in provenance chain"

examples_ref: null
