# W4/W6: Section retrieval — fact_vectors only (C0 evidence-only)
# Golden dense C0 sections: executive_summary, unify_bullets, unify_narrative,
# ibm_bullets, ibm_narrative, competencies (+ optional headline smoke).

schema_version: "2.1"
enabled: true

ssot_collection_strategy:
  primary_collection: fact_vectors
  embedding_model_id: BAAI/bge-m3
  embedding_dim: 1024
  persist_path_env: CHROMA_PERSIST_DIR
  default_persist_relative: data/cache/chromadb

collection: fact_vectors

fact_vectors_constraints:
  collection_name: fact_vectors
  allowed_source_classes_only:
    - candidate_profile
    - project_evidence

section_id_aliases:
  professional_summary: executive_summary

global_constraints:
  max_total_evidence_items: 40
  max_sections_to_query: 8
  max_query_budget_ms: 12000

source_authority_defaults:
  candidate_facts_as_proof: false
  candidate_fact_lineage_allowed: true
  graph_as_claim_proof: false
  graph_as_routing_support: true
  graph_claim_proof_allowed_only_when_fact_bound: true
  jd_as_proof_allowed: false
  briefing_as_proof_allowed: false
  companion_context_authority: false

# Phase 2 sparse/exact lane — mandatory for apps_rg when APPS_RG_C0_DENSE_SPARSE_MANDATORY=1 (default).
sparse_lane_defaults:
  sparse_enabled: true
  sparse_top_k: 5
  sparse_lane_id: ""
  sparse_collection_ref: fact_vectors
  sparse_index_ref: fact_vectors
  lexical_query_fields: []
  exact_match_fields: []
  dense_sparse_merge_policy: rrf
  sparse_unavailable_behavior: fail_closed
  sparse_empty_behavior: receipt_empty

sections:
  - retrieval_profile_id: apps_rg.headline.c0_profile
    section_id: headline
    display_name: Headline
    sparse_enabled: true
    sparse_lane_id: apps_rg.headline.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.headline
      - jd_payload.jd_text
    exact_match_fields:
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    headline_positioning_bundle_consumption: required
    graph_expansion_mode: headline_positioning_bundle_only
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile]
    query_fields:
      - resume_payload.headline
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_HEADLINE_ANCHOR principal product leader measurable revenue"

  - retrieval_profile_id: apps_rg.executive_summary.c0_profile
    section_id: executive_summary
    display_name: Executive summary
    sparse_enabled: true
    sparse_lane_id: apps_rg.executive_summary.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.executive_summary
      - resume_payload.summary
      - jd_payload.jd_text
    exact_match_fields:
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile]
    query_fields:
      - resume_payload.executive_summary
      - resume_payload.summary
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_EXEC_SUMMARY_ANCHOR concise narrative scope constraints outcomes"

  - retrieval_profile_id: apps_rg.competencies.c0_profile
    section_id: competencies
    display_name: "ENGINEERING & PLATFORM COMPETENCIES"
    sparse_enabled: true
    sparse_lane_id: apps_rg.competencies.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.competencies
      - resume_payload.skills
      - jd_payload.jd_text
    exact_match_fields:
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    competency_bundle_consumption: required
    graph_expansion_mode: competency_bundle_only
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile, project_evidence]
    query_fields:
      - resume_payload.competencies
      - resume_payload.skills
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_COMPETENCIES_ANCHOR Python asyncio PostgreSQL Redis Kubernetes"

  - retrieval_profile_id: apps_rg.unify_bullets.c0_profile
    section_id: unify_bullets
    display_name: Unify bullets
    sparse_enabled: true
    sparse_lane_id: apps_rg.unify_bullets.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.unify_bullets
      - resume_payload.experience
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    role_episode_bundle_consumption: required
    graph_expansion_mode: role_episode_bundle_only
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile]
    query_fields:
      - resume_payload.unify_bullets
      - resume_payload.experience
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_UNIFY_BULLETS_ANCHOR quantified bullets verbs metrics ownership"

  - retrieval_profile_id: apps_rg.unify_narrative.c0_profile
    section_id: unify_narrative
    display_name: Unify narrative
    sparse_enabled: true
    sparse_lane_id: apps_rg.unify_narrative.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.unify_narrative
      - resume_payload.summary
      - jd_payload.jd_text
    exact_match_fields:
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    role_episode_bundle_consumption: required
    graph_expansion_mode: role_episode_bundle_only
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile]
    query_fields:
      - resume_payload.unify_narrative
      - resume_payload.summary
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_UNIFY_NARR_ANCHOR cohesive story arc problem discovery execution"

  - retrieval_profile_id: apps_rg.ibm_bullets.c0_profile
    section_id: ibm_bullets
    display_name: IBM bullets
    sparse_enabled: true
    sparse_lane_id: apps_rg.ibm_bullets.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.ibm_bullets
      - resume_payload.experience
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    role_episode_bundle_consumption: required
    graph_expansion_mode: role_episode_bundle_only
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile]
    query_fields:
      - resume_payload.ibm_bullets
      - resume_payload.experience
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_IBM_BULLETS_ANCHOR enterprise platform delivery metrics ownership stakeholder alignment"

  - retrieval_profile_id: apps_rg.ibm_narrative.c0_profile
    section_id: ibm_narrative
    display_name: IBM narrative
    sparse_enabled: true
    sparse_lane_id: apps_rg.ibm_narrative.sparse
    sparse_collection_ref: fact_vectors
    lexical_query_fields:
      - resume_payload.ibm_narrative
      - resume_payload.summary
      - jd_payload.jd_text
    exact_match_fields:
      - jd_payload.jd_text
    dense_top_k: 3
    max_k: 3
    support_target: high
    allowed_collections: [fact_vectors]
    required_metadata_filters:
      app: apps_rg
    optional_metadata_filters: {}
    freshness_profile: default
    authority_classes_allowed: [PRIMARY, SUPPORTING, REFERENCE]
    graph_expansion_allowed: true
    source_authority:
      candidate_facts_as_proof: false
      candidate_fact_lineage_allowed: true
      graph_as_claim_proof: false
      graph_as_routing_support: true
      graph_claim_proof_allowed_only_when_fact_bound: true
      jd_as_proof_allowed: false
      briefing_as_proof_allowed: false
      companion_context_authority: false
    role_episode_bundle_consumption: required
    graph_expansion_mode: role_episode_bundle_only
    fallback_policy: use_fallback_queries_if_query_fields_empty
    expected_evidence_strata: [CANONICAL]
    source_class_allowlist: [candidate_profile]
    query_fields:
      - resume_payload.ibm_narrative
      - resume_payload.summary
      - jd_payload.jd_text
    fallback_queries:
      - "SMOKE_C0_IBM_NARR_ANCHOR enterprise stakeholder narrative scope outcomes verification"

unavailability_behavior:
  support_status: WEAK_WITH_CAVEATS
  reason: "fact_vectors collection unavailable for section retrieval"

rejected_source_classes:
  - company_research
  - rubrics
  - governance_docs
  - approved_examples
  - receipts
  - process_docs
