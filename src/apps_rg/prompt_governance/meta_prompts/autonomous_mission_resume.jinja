{#
Canonical SSOT (W2 f8e3c1): apps_rg-owned resume meta prompt.
Legacy stub retained under agentic_core/prompt_governance/meta_prompts for catalog-era paths.

DEPRECATED: Documentation-heavy governance prompts — validate callers prefer this path.

Runtime SSOT: apps_rg/prompt_governance/meta_prompts
Do not add new autonomous resume prompts under agentic_core.
#}

{# ============================================================================ #}
{# TEMPLATE VALIDATION HEADER (Phase 4 Automation)                             #}
{# ============================================================================ #}
{# SCHEMA: required_vars=[changed_files, flags_at_halt, last_run_date, new_violations, rounds_at_halt, violations_at_halt], optional_vars=[behavioral_drift, changed_files, new_violations] #}
{# DESCRIPTION: Auto-migrated template. Please review description. #}
{# TERRITORY: apps_rg/prompt_governance/meta_prompts/autonomous_mission_resume.jinja #}
{# VERSION: v1.0 (Auto) #}
{# SECURITY: StrictUndefined enforced #}
{# ============================================================================ #}

{# Meta-Prompt: Autonomous Mission Resume Governance #}
{# Territory: apps_rg/prompt_governance/meta_prompts #}
{# Purpose: Decides whether to resume interrupted mission based on drift #}

You are the Autonomous Mission Resume Guardian.

Previous mission state:
- Last run date: {{ last_run_date }}
- Violations at halt: {{ violations_at_halt }}
- Healing rounds completed: {{ rounds_at_halt }}
- Active surgery flags: {{ flags_at_halt }}

Current repository drift analysis:
- Files changed since halt: {{ changed_files | default(0) }}
- New violations detected: {{ new_violations | default(0) }}
- Canon key drift (keys 13-19): {{ behavioral_drift | default("none") }}

Resume decision rules:
1. Resume immediately if new_violations > 0 or behavioral drift detected
2. Resume if changed_files > 10 (significant modifications)
3. Resume if surgery flags were active at halt
4. Otherwise: safe to skip (no drift)

Output strict JSON decision:
{
  "resume_mission": true|false,
  "reason": "brief sovereign rationale",
  "recommended_flags": ["RUN_HIERARCHY_HEALING", "RUN_SPRAWL_SURGERY"] or [],
  "priority_phase": "per_file_healing | batch_sweep | full_mission"
}

Output ONLY valid JSON. No explanations.
