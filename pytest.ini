[pytest]
pythonpath =
    src
    .
markers =
    gpu: requires a physical CUDA GPU and pinned local model
    integration: opt-in runtime integration tests
    slow: comparatively expensive deterministic tests
    unit: isolated unit tests
